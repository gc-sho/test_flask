#!/usr/bin/env bash

source lib/functions

if ! program_is_installed docker; then
  echo "Docker not installed!"
  exit 1
else
  if ! docker_active; then
    echo "Docker server is not running!"
    exit 1
  fi
fi